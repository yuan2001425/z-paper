<div align="center">
  <img src="icon.svg" width="96" alt="z-paper logo" />
  <p>本地部署的学术论文精准翻译与 AI 知识库对话工具，专为个人研究者设计。</p>
  <p><a href="README_EN.md">English</a> | 中文</p>
</div>

所有数据保存在本地，不依赖任何外部数据库或消息队列服务。

---

## 截图

<table>
  <tr>
    <td><img src="images/Snipaste_2026-04-22_03-03-35.png" alt="主页"/><br/><sub>主页</sub></td>
    <td><img src="images/Snipaste_2026-04-22_03-02-11.png" alt="论文库与虚拟文件夹"/><br/><sub>论文库 · 虚拟文件夹</sub></td>
  </tr>
  <tr>
    <td><img src="images/Snipaste_2026-04-22_03-02-34.png" alt="双栏译文阅读"/><br/><sub>双栏译文阅读</sub></td>
    <td><img src="images/Snipaste_2026-04-22_03-02-49.png" alt="公式渲染"/><br/><sub>LaTeX 公式渲染</sub></td>
  </tr>
  <tr>
    <td colspan="2"><img src="images/Snipaste_2026-04-22_03-03-24.png" alt="AI 知识库对话与图片生成"/><br/><sub>AI 知识库对话 · 图片生成</sub></td>
  </tr>
</table>

---

## 下载安装（Windows）

> ⚠️ **百度网盘的安装包版本已过时，不再维护，功能远落后于当前版本。**  
> 推荐直接通过源码安装（见下方「快速启动」），步骤简单，两条命令即可运行。

**百度网盘（旧版，仅供参考）：** https://pan.baidu.com/s/1ZdRe2TixlulmjIyZI0UEzQ?pwd=6zqp  提取码：6zqp

---

## 一、精准翻译

学术论文翻译的核心难题不是语言，而是**术语一致性**和**排版还原**。z-paper 通过六阶段并行流水线解决这两个问题。

```
上传 PDF
  → 重复检测（与已有论文标题相似度对比，避免重复收录）
  → MinerU 解析（识别多栏/公式/表格/图注）
  → 并行文本清理（8 并发）：合并 PDF 断行、修正 OCR 错误、规范 LaTeX 格式
  → 术语提取：自动识别领域专有名词
  → [暂停] 用户审查新术语，确认每个词的译法（仅翻译 / 保留原文 / 翻译并标注原文）
  → 带术语表并行翻译段落（8 并发，DeepSeek）+ 图片翻译（3 并发，Qwen）
  → 生成双栏译文
```

**关键设计：术语审查检查点**

大多数翻译工具在遇到新术语时会随机翻译，导致同一术语在论文不同位置出现不同译名。z-paper 在翻译前强制暂停，让用户确认每一个新术语的译法，确认后保存到个人词库，后续所有翻译自动复用，保证全文一致。

**双层词汇系统**

- **个人词库**：跨论文通用，自定义每个术语的处理策略
- **领域词库**：11 个预设学科（计算机科学、数学、物理学、生物学等）公用参考词表
- 每次翻译快照词库状态，译文可复现

**阅读体验**

- 中英双栏并排，支持切换单栏
- LaTeX 数学公式 KaTeX 实时渲染
- 图片 Lightbox 放大，原图与译图对比
- 文内引用 `[1]` 可点击跳转参考文献列表
- 精确到段落的划选批注系统
- **一键导出 DOCX**：支持导出原文 / 译文 / 原文+译文三种模式，自动嵌入图片、渲染 LaTeX 公式为 Word 原生数学公式，窄边距紧凑排版

---

## 二、知识库对话

### 设计思想：工具优先，而非塞满 Context

目前大多数"论文问答"产品的做法是 RAG：把检索出的文本块塞进 context，让模型回答。这个方案有明显上限——检索质量决定回答质量，模型没有主动探索的能力。

z-paper 的设计思想借鉴自 **[Claude Code](https://claude.ai/code)**：**不预先决定给模型看什么，而是给 Agent 配备一套工具，让模型自主决定每一步该读哪里、读多少**。Context 是稀缺资源，Agent 的价值在于它知道如何高效使用它。

### 两个关键分离

**位置与内容分离**

工具返回的内容不需要永久驻留在 context 里。压缩时只保留索引（在哪里找到了什么），丢弃正文。正文随时可以通过工具重新获取——它是幂等的。这和人读论文的方式一样：你不会把整篇论文背下来，但你记得"第三节讲过这个，去翻一下"。

**检索与阅读分离**

搜索类工具负责定位，读取类工具负责精读。Agent 先定位、再按需深读，而不是一次性把所有内容塞进来。

### 14 个工具构成完整的导航与创作能力

| 工具 | 功能 |
| --- | --- |
| `search_papers` | 在全库元数据（标题/摘要/关键词）中搜索 |
| `get_paper_outline` | 获取论文章节大纲 |
| `get_paper_metadata` | 快速获取论文摘要/作者/关键词 |
| `search_in_paper` | 在单篇论文全文中关键词检索 |
| `search_across_papers` | **跨全库**全文搜索，一次扫遍所有论文 |
| `get_paper_section` | 获取指定章节**完整文本**（无段落数量限制） |
| `get_paragraph_context` | 获取某段落的前后文 |
| `get_references` | 获取参考文献列表 |
| `get_annotations` | 获取某篇论文的所有个人批注 |
| `search_annotations` | **跨全库**搜索个人批注（中文分词 + 拼音匹配） |
| `search_chat_history` | 搜索历史对话记录 |
| `query_database` | 执行自定义 SQL 查询（最后手段，处理其他工具无法表达的复杂查询） |
| `generate_image` | 根据描述生成图片（文生图，可视化论文概念/流程/对比） |
| `edit_image` | 对已有图片按指令修改 |

Agent 每一步自主决定调哪个工具、调几次，直到认为信息足够才输出回答。没有固定流程，调用策略完全由模型根据问题推理。

### AI 图片生成

对话时可直接要求 Agent 生成图表，例如"用图示说明 Transformer 的注意力机制"。Agent 会调用 `generate_image` 工具，将生成的图片内嵌在回答中。图片支持：

- 点击放大，鼠标滚轮缩放（0.5× ~ 5×），放大后可拖拽平移
- 一键下载
- 点击"编辑图片"后输入修改指令，Agent 调用 `edit_image` 对原图进行调整

### 两层记忆架构

- **L1 工作记忆**：当前对话上下文，每轮自动注入论文目录和个人词表
- **L2 冷数据**：全部历史对话持久化在本地数据库，Agent 通过 `search_chat_history` 工具按需检索。每个会话完全独立，不自动注入历史，避免错误结论跨会话污染

### 五级上下文压缩，支持超长研究会话

| 级别 | 触发条件 | 操作 | API 开销 |
| --- | --- | --- | --- |
| L1 剪裁 | >4 轮 | 旧工具结果压缩为结构索引（保留"在哪找到"，丢弃正文） | 无 |
| L2 微压缩 | >8 轮 | 旧工具调用折叠为单行摘要 | 无 |
| L3 折叠 | >12 轮 | 旧对话内容截短 | 无 |
| L4 自动压缩 | >16 轮 | LLM 生成全量摘要替换旧轮次 | 有 |
| L5 应急压缩 | 超 80K 字符 | 强制保留最近 3 轮 | 无 |

熔断器：L4 连续失败 3 次后停止重试，不浪费 API 调用。

### 其他细节

- **双语搜索**：中文提问时自动同时搜索中英文关键词，中文人名还会尝试拼音，最大化召回率
- **流式输出**：工具调用阶段非流式保证 JSON 格式准确，最终回答 SSE 流式推送，实时展示每步工具调用过程
- **引用溯源**：每条回答附带引用卡片，点击后打开原文并**自动滚动定位到对应段落**（精确到 block 级别）
- **会话 Minimap**：对话页面右侧缩略导航栏，实时追踪当前位置

---

## 技术栈

| 层 | 技术 | 说明 |
| --- | --- | --- |
| 后端框架 | FastAPI + Uvicorn | 异步 HTTP + WebSocket 实时进度推送 |
| 数据库 | SQLite（WAL 模式） | 零依赖，无需安装任何数据库服务 |
| ORM | SQLAlchemy 2.0 | |
| 异步 | Python asyncio | 无 Celery / Redis，纯线程池并发 |
| 前端框架 | Vue 3 + Vite 5 | |
| UI 组件库 | Element Plus | |
| 公式渲染 | KaTeX | |
| 翻译 LLM | DeepSeek Chat | 段落翻译 + Agent 对话 |
| 视觉 LLM | Qwen3-VL-Flash / Qwen-Image-2.0-Pro / Qwen-Image-2.0 | 文字处理 + 图片翻译 + 图片生成 |
| PDF 解析 | MinerU API | |

---

## 快速启动（源码）

**系统要求：** Python 3.11+、Node.js 18+

```bash
git clone https://github.com/yuan2001425/z-paper.git
cd z-paper

# Windows
start.bat

# Linux / macOS
bash start.sh
```

首次启动跳转配置页，填写三个 API Key：

| Key | 用途 | 申请 |
| --- | --- | --- |
| DeepSeek API Key | 翻译 + 对话 | https://platform.deepseek.com |
| 通义千问 API Key | 图片识别 + 文本处理 + 图片生成 | https://dashscope.console.aliyun.com |
| MinerU API Key | PDF 解析 | https://mineru.net |

---

## 数据说明

所有数据完全保存在本地：`backend/data/zpaper.db`（数据库）和 `backend/uploads/`（文件）。备份这两个目录即可完整迁移。

### 同 WLAN 数据同步

两台电脑连接同一个 WLAN 并分别启动 z-paper 后，可在「配置」页底部使用数据同步：

1. 在接收端点击「打开检测」，让本机短时间内可被同 WLAN 设备发现。
2. 在发送端点击「扫描设备」，选择扫描到的接收端并发送同步申请。
3. 在接收端「待同意申请」中点击「同意并导入」，前端会显示清单比对、打包、传输和导入日志。

同步前会检查两端 z-paper 版本，版本不一致会拒绝导入。同步内容包括 API 配置、论文库、译文结果、批注、对话历史、词库、文件夹和上传文件。系统会先比对来源端和接收端的数据清单，只打包接收端缺失的数据库记录和上传文件；上传文件会按相对路径、大小和 SHA-256 指纹判断是否已存在，同路径但内容不同的文件会作为冲突跳过，不覆盖接收端已有文件。API 配置会更新为来源端配置。导入前会把接收端旧数据库备份到 `backend/data/sync_backups/`。

如果扫描不到设备，请检查系统防火墙是否允许 z-paper / Python / uvicorn 访问局域网，并放行 TCP `8000` 与 UDP `37621`。打包后的生产模式和源码开发模式都支持同步；源码开发模式下另一台电脑也可以访问 `http://<本机 IP>:3000`，同步 API 仍通过后端 `8000` 端口完成。

---

## 更新日志

### v2.2.1
- **已知问题修复**：修复同 WLAN 同步大量 PDF 时，超长论文文件名在 Windows 临时目录中导致同步包解压或校验失败的问题。新版同步包内部改用短文件路径，原始文件路径通过清单映射恢复。
- **同步容错增强**：导出前会跳过来源端已经丢失的上传文件；导入时若个别包内文件缺失，会记录跳过数量并继续导入其他数据，避免 800MB 级同步包因单个文件异常整体失败。
- **版本校验更新**：同 WLAN 同步版本升至 `2.2.1`，两台电脑需安装同一版本后再同步。

### v2.2.0
- **同 WLAN 数据同步**：接收端打开检测后，发送端可扫描同 WLAN 内可用设备并发送同步申请，无需手动填写目标地址。
- **差异同步**：同步前先比对来源端和接收端清单，只打包接收端缺失的数据库记录和上传文件；同路径但内容不同的上传文件会作为冲突跳过，避免覆盖接收端已有 PDF。
- **版本校验与交互进度**：同步前校验两端版本一致；传输和导入过程会在前端显示进度、日志和错误信息。
- **接收端确认与备份**：接收端必须手动同意才会导入；导入前会将接收端旧数据库备份到 `backend/data/sync_backups/`，并拒绝在识别/翻译任务运行中同步。
- **局域网启动支持**：Windows、Linux/macOS 启动脚本和 Vite 开发服务器默认监听局域网地址，便于同 WLAN 设备访问。
- **已知问题修复**：修复打包版在中文 Windows 下打开检测可能返回 500、覆盖安装后旧后台服务未停止，以及导入完成弹窗出现多余“刷新页面”按钮的问题。

### v2.1.0
- **失败任务重新开始**：任务列表中的失败短文档可点击“重新开始”，系统会使用原始 PDF 重新进入识别/翻译或中文归档流程。
- **长文档章节重跑**：长文档详情页支持单独重跑失败章节；父级长文档失败时，也可一键重新开始所有失败章节，完成后继续自动汇总合并结果。
- **重试安全性**：重新开始只对失败任务开放，重跑前会清理该任务残留错误状态和旧结果，不会新建重复论文记录，也不会影响 `backend/uploads/` 与 `backend/data/` 的隐私忽略规则。

### v2.0.0
- **长文档翻译模式**：英文上传、中文上传和批量上传都可以标记为长文档。长文档会先保存为父级草稿，不会立刻进入识别/翻译流程。
- **手动分章**：上传后进入分章页，按页码填写章节范围，支持单页缩略图预览，并校验页码越界、起止页错误和章节重叠；未覆盖的封面、目录等页面会被跳过。
- **逐章队列处理**：确认分章后，系统用 PyMuPDF 将原 PDF 拆成多个章节 PDF，每章按普通英文翻译或中文归档任务进入现有队列。
- **父级汇总结果**：章节全部完成后，父级长文档自动合并正文、章节标题、文档记忆和去重后的参考文献，论文库默认只展示父级长文档，不展示拆出的章节。
- **隐私与存储**：原文、章节 PDF 和本地数据库仍保存在 `backend/uploads/` 与 `backend/data/`，这两个目录继续被 Git 忽略，不会随源码提交。

### v1.4.0
- **长文档翻译记忆**：在结构分析后构建紧凑的文档级记忆，包括章节大纲、章节上下文、缩写、符号、高频概念和适用术语；逐段翻译时只注入相关局部上下文，提升超长文档的术语一致性和跨章节衔接，同时不影响 PDF 解析流程。

### v1.3.0
- **DOCX 导出**：译文阅读页一键导出，三种模式（原文 / 译文 / 原文+译文），自动嵌入图片、LaTeX 公式渲染为 Word 原生数学公式（OMML），窄边距紧凑排版，文件名自动标注模式 `{中文标题}(模式).docx`

### v1.2.0
- **虚拟文件夹**：论文库支持多级分类（最多三级），文件夹树形导航，拖拽移动论文；删除文件夹可选择保留论文（移入未分类）或同时删除文件
- **批量上传**：一次选取多篇 PDF，支持逐篇自动提取元数据、重复检测、独立确认/跳过，最后统一提交
- **论文库批量管理**：多选论文，一键批量移动到文件夹

### v1.1.0
- **对话图片生成**：新增 `generate_image` / `edit_image` 工具，Agent 可生成图片并内嵌回答；图片查看器支持滚轮缩放与拖拽平移
- **重复检测**：上传论文前自动比对标题相似度，防止重复收录
- **论文库优化**：瀑布流（masonry）布局，懒加载，卡片高度独立不对齐
- **PDF 元数据提取**：修复大文件卡住的问题

### v1.0.0
- 初始发布

---

## 许可证

MIT
